# Authentication monitoring cron jobs

# Health check every 5 minutes
*/5 * * * * monitoring/health-check.sh >> /var/log/auth-health.log 2>&1

# Log monitoring every minute  
* * * * * monitoring/monitor-logs.sh >> /var/log/auth-monitor.log 2>&1

# Performance check every 15 minutes
*/15 * * * * monitoring/performance-check.sh >> /var/log/auth-performance.log 2>&1

# Daily validation script
0 2 * * * monitoring/../scripts/validate-auth-environment.sh >> /var/log/auth-validation.log 2>&1
